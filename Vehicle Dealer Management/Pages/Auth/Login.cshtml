@page "/Login"
@model Vehicle_Dealer_Management.Pages.Auth.LoginModel
@{
    Layout = "_LayoutPublic";
    ViewData["Title"] = "Đăng nhập";
}

<div class="content" style="display: flex; align-items: center; justify-content: center; min-height: calc(100vh - 80px);">
    <div class="card" style="max-width: 480px; width: 100%;">
        <div class="card-header text-center">
            <h2 class="card-title">Đăng nhập</h2>
            <p class="card-subtitle">Truy cập vào hệ thống EVM Dealer Portal</p>
        </div>
        
        <div class="card-body">
            <form method="post">
                <div class="form-group">
                    <label class="form-label" for="email">Email</label>
                    <input type="email" class="form-control" id="email" name="email" value="@Model.Email" placeholder="you@example.com" required />
                </div>

                <div class="form-group">
                    <label class="form-label" for="password">Mật khẩu</label>
                    <input type="password" class="form-control" id="password" name="password" placeholder="••••••••" required />
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block">
                        <i class="bi bi-box-arrow-in-right"></i>
                        Đăng nhập
                    </button>
                </div>

                @if (Model.ErrorMessage != null)
                {
                    <div class="alert alert-danger" style="background: var(--error-bg); color: var(--error); border: 1px solid var(--error); padding: 1rem; border-radius: var(--radius-sm); margin-top: 1rem;">
                        <i class="bi bi-exclamation-triangle"></i> @Model.ErrorMessage
                    </div>
                }
            </form>
        </div>

        <div class="card-footer text-center">
            <p class="text-muted mb-2">Chưa có tài khoản? <a href="/Register">Đăng ký ngay</a></p>
            <hr style="border-color: var(--border); margin: 1rem 0;" />
            
            <div style="background: var(--black); padding: 1rem; border-radius: var(--radius-sm); margin-top: 1rem;">
                <p class="text-muted mb-3" style="font-size: 0.875rem; font-weight: 600;">
                    <i class="bi bi-key"></i> Test Accounts (Password: <code style="color: var(--accent-cyan);">123456</code>)
                </p>
                
                <div style="display: grid; gap: 0.5rem; text-align: left;">
                    <div style="display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem; background: rgba(255,255,255,0.02); border-radius: var(--radius-sm); cursor: pointer;" onclick="fillLogin('customer@test.com', '123456')" title="Click để điền thông tin">
                        <div style="width: 32px; height: 32px; border-radius: 50%; background: var(--accent-cyan); display: flex; align-items: center; justify-content: center; color: var(--black); font-weight: 600;">👤</div>
                        <div style="flex: 1;">
                            <div style="font-size: 0.875rem; font-weight: 600;">Customer</div>
                            <code style="color: var(--accent-cyan); font-size: 0.75rem;">customer@test.com</code>
                        </div>
                        <i class="bi bi-clipboard" style="color: var(--text-muted); font-size: 0.875rem;"></i>
                    </div>

                    <div style="display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem; background: rgba(255,255,255,0.02); border-radius: var(--radius-sm); cursor: pointer;" onclick="fillLogin('dealerstaff@test.com', '123456')" title="Click để điền thông tin">
                        <div style="width: 32px; height: 32px; border-radius: 50%; background: var(--success); display: flex; align-items: center; justify-content: center; color: var(--black); font-weight: 600;">💼</div>
                        <div style="flex: 1;">
                            <div style="font-size: 0.875rem; font-weight: 600;">Dealer Staff</div>
                            <code style="color: var(--accent-cyan); font-size: 0.75rem;">dealerstaff@test.com</code>
                        </div>
                        <i class="bi bi-clipboard" style="color: var(--text-muted); font-size: 0.875rem;"></i>
                    </div>

                    <div style="display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem; background: rgba(255,255,255,0.02); border-radius: var(--radius-sm); cursor: pointer;" onclick="fillLogin('dealermanager@test.com', '123456')" title="Click để điền thông tin">
                        <div style="width: 32px; height: 32px; border-radius: 50%; background: var(--warning); display: flex; align-items: center; justify-content: center; color: var(--black); font-weight: 600;">👔</div>
                        <div style="flex: 1;">
                            <div style="font-size: 0.875rem; font-weight: 600;">Dealer Manager</div>
                            <code style="color: var(--accent-cyan); font-size: 0.75rem;">dealermanager@test.com</code>
                        </div>
                        <i class="bi bi-clipboard" style="color: var(--text-muted); font-size: 0.875rem;"></i>
                    </div>

                    <div style="display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem; background: rgba(255,255,255,0.02); border-radius: var(--radius-sm); cursor: pointer;" onclick="fillLogin('evmstaff@test.com', '123456')" title="Click để điền thông tin">
                        <div style="width: 32px; height: 32px; border-radius: 50%; background: var(--info); display: flex; align-items: center; justify-content: center; color: var(--black); font-weight: 600;">🏭</div>
                        <div style="flex: 1;">
                            <div style="font-size: 0.875rem; font-weight: 600;">EVM Staff</div>
                            <code style="color: var(--accent-cyan); font-size: 0.75rem;">evmstaff@test.com</code>
                        </div>
                        <i class="bi bi-clipboard" style="color: var(--text-muted); font-size: 0.875rem;"></i>
                    </div>

                    <div style="display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem; background: rgba(255,255,255,0.02); border-radius: var(--radius-sm); cursor: pointer;" onclick="fillLogin('admin@test.com', '123456')" title="Click để điền thông tin">
                        <div style="width: 32px; height: 32px; border-radius: 50%; background: var(--accent-cyan); display: flex; align-items: center; justify-content: center; color: var(--black); font-weight: 600;">👑</div>
                        <div style="flex: 1;">
                            <div style="font-size: 0.875rem; font-weight: 600;">EVM Admin</div>
                            <code style="color: var(--accent-cyan); font-size: 0.75rem;">admin@test.com</code>
                        </div>
                        <i class="bi bi-clipboard" style="color: var(--text-muted); font-size: 0.875rem;"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function fillLogin(email, password) {
        document.querySelector('input[name="email"]').value = email;
        document.querySelector('input[name="password"]').value = password;
        
        // Optional: Auto submit or show feedback
        const emailInput = document.querySelector('input[name="email"]');
        emailInput.focus();
        emailInput.select();
        
        // Show brief feedback
        const btn = event.currentTarget;
        const originalBg = btn.style.background;
        btn.style.background = 'rgba(155, 234, 251, 0.2)';
        setTimeout(() => {
            btn.style.background = originalBg;
        }, 500);
    }
</script>

